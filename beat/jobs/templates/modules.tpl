{% comment %}
one day this template will do something like
	module load ltsmin/<version>
but since the module loading doesn't appear to be working too well, instead this file contains nothing.
{% endcomment %}